name: protocol
description: Wire protocol / message types shared by server and client.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  sim:
    path: ../sim

dev_dependencies:
  test: ^1.25.0
